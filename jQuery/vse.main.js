var recognition = new webkitSpeechRecognition(); recognition.onresult = function(event) {  var saidText = ""; for (var i = event.resultIndex; i < event.results.length; i++) { if (event.results[i].isFinal) {  saidText = event.results[i][0].transcript; } else {saidText += event.results[i][0].transcript;}} document.getElementById('speechText').value = saidText; document.getElementById('start').click(); searchPosts(saidText);} 
function startDictation(){ recognition.start(); } function searchPosts(saidText){ $.ajax({ url: '/', type: 'get', data: {find: saidText}, success: function(response){  $('.container').empty();  $('.container').append(response); } });}